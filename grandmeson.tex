\documentclass[11pt]{article}

\usepackage[a4paper,margin=27mm]{geometry}
\usepackage{amsmath,amssymb,amsthm,mathtools}
\usepackage{enumitem}
\usepackage{xcolor}
\usepackage{hyperref}

\hypersetup{
  colorlinks=true,
  linkcolor=blue!50!black,
  citecolor=blue!50!black,
  urlcolor=blue!50!black
}

\setlength{\emergencystretch}{2em}

\theoremstyle{definition}
\newtheorem{definition}{Definition}
\newtheorem{remark}[definition]{Remark}
\newtheorem{convention}[definition]{Convention}
\newtheorem{axiom}[definition]{Axiom}

\theoremstyle{plain}
\newtheorem{proposition}[definition]{Proposition}

\DeclareMathOperator{\supp}{supp}
\DeclareMathOperator{\Read}{Read}
\DeclareMathOperator{\Cl}{Cl}
\DeclareMathOperator{\Top}{Top}
\DeclareMathOperator{\Dow}{Dow}
\DeclareMathOperator{\Contr}{Contr}
\DeclareMathOperator{\Mor}{Mor}
\DeclareMathOperator{\op}{op}
\DeclareMathOperator{\Fis}{Fis}
\DeclareMathOperator{\ClosedResidue}{ClosedResidue}
\DeclareMathOperator{\Buried}{Buried}
\DeclareMathOperator{\RCl}{RCl}
\newcommand{\qu}{\mathrm u}
\newcommand{\qd}{\mathrm d}
\newcommand{\qc}{\mathrm c}
\newcommand{\qs}{\mathrm s}
\newcommand{\qt}{\mathrm t}
\newcommand{\qb}{\mathrm b}

\title{The Grand Meson}
\author{Shuhei Ihara}
\date{\today}

\begin{document}
\maketitle

\begin{abstract}
This paper states a precise operational thesis: the Grand Meson is the
update procedure that tests how far the meson concept can be stretched after
the six quark flavors are known, while still recovering the older mesonic
face without discontinuity.  The starting point is the General Meson: a
two-face Yukawa-style mediator read, in modern quark language, through the
light pair \(\qu\leftrightarrow\qd\).  The Grand Meson opens the two
remaining readable quark-pair axes and thereby becomes a three-axis,
six-face skeleton whose physical completion is the \(36\) oriented
\(q\bar r\)-channel mesonic body.  The extension is conservative: the
original two-face meson is recovered by projection, while the new faces and
body channels are resolved as open, inactive, or active instead of being
left outside the channel basis.  More precisely, the General Meson is the
substructure of the Grand Meson in which every non-UD face and every non-UD
body channel is buried as a closed inactive residual package.  The observed
three quark generations are represented as internal Grand Meson directions.
Backward compatibility is surjective, but downgrading with unclosed residue
is blocked as a Grand Meson contradiction.  In this paper,
``generalization'' means conservative channel-completion with exact
recovery, not replacement of QCD; ``admissible'' means closed under the
stated mesonic channels; and ``fission'' means a contradictory downgrade
pattern of residual channels, not nuclear fission.  The central claim is
therefore strong but bounded: for the present six-flavor quark sector, the
Grand Meson is an update manual and a continuity test for mesonic elasticity
and robustness.
\end{abstract}

\section{Opening}

A meson is usually approached through its physical role: it mediates an
interaction.  In the present paper the same word is first read through its
paired role.  A meson stands between two complementary light-flavor faces.
In the Yukawa-facing reconstruction used here, that channel pair is
\[
  \qu\leftrightarrow\qd.
\]
This is not a quark--quark bound state.  It is the light-flavor channel face
from which the QCD readout later forms \(q\bar r\) channels such as
\(\qu\bar{\qd}\), \(\qd\bar{\qu}\), \(\qu\bar{\qu}\), and
\(\qd\bar{\qd}\).  Yukawa did not formulate his meson with quark flavors;
the notation is a present reconstruction of the light pair behind the
proton--neutron mediation problem.

The former working nickname ``Ethon'' is retired here.  The technical
object of this paper is the Grand Meson.  The name is structural, not
honorific: ``grand'' means that a two-channel light-pair meson has been closed
into the admissible three-axis case.

\section{Operational Thesis}

The Grand Meson is not introduced as a new bound state and not as a
replacement for the standard quark--antiquark meson.  It is introduced as a
channel-completion operation on the mesonic flavor sector.  Its purpose is
to make the following claim explicit:
\[
  \text{mesonic elasticity}
  =
  \text{extension to six flavor faces without loss of recovery}.
\]
The corresponding robustness condition is exact projection back to the
older face:
\[
  \text{mesonic robustness}
  =
  \bigl(p_{\qu\qd}(g)=m\bigr)
  \ \wedge\
  \ClosedResidue_{g\Downarrow m}\bigl(R_{\perp}(g)\bigr).
\]
Thus the paper is an update manual for mesonic channel operations and a
verification of operational continuity.  A Grand Meson statement is
well formed only when the older General Meson face is recovered and every
residual channel is closed.

\begin{remark}[Historical status]
The pair \(\qu\leftrightarrow\qd\) is not claimed to be Yukawa's original
notation.  It is a modern light-flavor reconstruction of the mediation
problem that Yukawa isolated.  The historical input is the mediator problem;
the Grand Meson construction is the present six-flavor completion procedure.
\end{remark}

\section{Standard Mesonic Channel Data}

Let the six known quark flavors be
\[
  F_6=\{\qu,\qd,\qc,\qs,\qt,\qb\}.
\]
The physical body of a mesonic channel is not a single flavor face but an
oriented quark--antiquark channel
\[
  (q,\bar r)\in F_6\times \overline{F_6}.
\]
For a spin-parity sector \(\sigma\), let \(\Gamma_\sigma\) be the
corresponding Dirac structure and define the standard color-singlet
interpolating bilinear
\[
  O^{\Gamma_\sigma}_{q\bar r}(x)
  :=
  \sum_{a=1}^{3}\bar r_a(x)\Gamma_\sigma q^a(x).
\]
When the spin-parity sector is fixed or irrelevant to the displayed
formula, write \(O_{q\bar r}\) for \(O^{\Gamma_\sigma}_{q\bar r}\).
Let \(Q_q\) be the electric charge of \(q\), and let
\(e_q\in\mathbb Z^{F_6}\) be the flavor unit at \(q\).  For a spin-parity
sector \(\sigma\), define the QCD quantum fingerprint
\[
  \nu_{\mathrm{QCD}}(q,\bar r;\sigma)
  :=
  (\sigma,\ Q_q-Q_r,\ e_q-e_r,\ \mathsf B_{\mathrm{ary}}=0,\
  \mathbf 1_{\mathrm c}).
\]
Here \(\mathsf B_{\mathrm{ary}}\) denotes baryon number and is not the
bottom-flavor label \(\qb\).

\begin{definition}[Mesonic polymer package]
For \(q,r\in F_6\), a mesonic polymer is the channel package
\[
  P_{q\bar r}
  :=
  (O^{\Gamma_\sigma}_{q\bar r},\ J^{q\bar r}_{\sigma},\
    \nu_{\mathrm{QCD}}(q,\bar r;\sigma),\
    C_{q\bar r,\bullet},\
    K_{\mathrm{QCD}}(q\bar r)).
\]
Here \(J^{q\bar r}_{\sigma}\) is the source coefficient for the
Dirac/spin-parity sector,
\(C_{q\bar r,\bullet}\) denotes the corresponding correlator family, and
\(K_{\mathrm{QCD}}\) denotes the ordinary QCD conservation data.  Set
\[
  \mathsf{Pol}_{q\bar r}
  :=
  \{\,P_{q\bar r}\mid P_{q\bar r}\ \text{satisfies the closure suite below}\,\},
  \qquad
  \mathsf{Pol}
  :=
  \bigsqcup_{q,r\in F_6}\mathsf{Pol}_{q\bar r}.
\]
Thus \(\mathsf{Pol}\) is not an arbitrary symbol set.  It is the disjoint
union of closed source--operator--quantum-number--correlator packages for
oriented mesonic channels.
\end{definition}

\begin{definition}[Grand Meson closure suite]
Let
\[
  \Cl_{\mathrm{GM}}
  :=
  \{\mathrm{Color}_1,\mathrm{Lorentz},\mathrm{Charge},
    \mathrm{Flavor},\mathrm{CKM},\mathrm{Top},
    \mathrm{Threshold},\mathrm{Observable}\}.
\]
For a channel package \(P\), \(\Cl(P)\subseteq\Cl_{\mathrm{GM}}\) is the
set of closure tests satisfied by \(P\).  A package is Grand-closed when
\[
  \Cl(P)=\Cl_{\mathrm{GM}}.
\]
For a downgrade or transition symbol \(s\), \(\RCl(s)\) denotes the
residual faces or channels closed by that symbol.  Thus \(\Cl(P)\) is a
test-suite value of a package, while \(\RCl(s)\) is a residual-closure set
attached to an operation symbol; the two types are not identified.
The tests mean, respectively: color singletness, Lorentz covariance,
electric-charge conservation, flavor bookkeeping, weak charged-current CKM
closure, top-axis weak-virtual closure, threshold or decay closure, and an
observable readout.  A Grand Meson contradiction is therefore not a private
logical label; it is a failure of a proposed channel symbol to inhabit the
standard-model-compatible closure suite.
\end{definition}

\begin{definition}[Inactive residual package]
Let
\[
  \mathsf{Prov}_{0}
  :=
  \{\mathrm{selection},\mathrm{kinematic},\mathrm{coupling},
    \mathrm{sensitivity},\mathrm{integrated},\mathrm{buried}\}.
\]
For each oriented channel \(q\bar r\), define a closed inactive package
\[
  \mathbf 0_{q\bar r}[\rho],
  \qquad
  \rho\in\mathsf{Prov}_{0}.
\]
It is typed by the channel \(q\bar r\), carries zero source and zero
ordinary readout contribution, and satisfies the Grand Meson closure suite
as an inactive residual package:
\[
  \Cl(\mathbf 0_{q\bar r}[\rho])=\Cl_{\mathrm{GM}}.
\]
Set
\[
  \mathsf{Pol}^{\mathrm{res}}_{q\bar r}
  :=
  \mathsf{Pol}_{q\bar r}\cup
  \{\,\mathbf 0_{q\bar r}[\rho]
  \mid
  \rho\in\mathsf{Prov}_{0}\,\},
  \qquad
  \mathsf{Pol}^{\mathrm{res}}
  :=
  \bigsqcup_{q,r\in F_6}\mathsf{Pol}^{\mathrm{res}}_{q\bar r}.
\]
At the face-skeleton level we write \(\mathbf 0_q[\rho]\) for the
corresponding closed inactive family of packages incident to the face \(q\).
\end{definition}

\section{The General Meson}

Let the Yukawa-facing readable pair be
\[
  A_Y=\{\qu,\qd\}.
\]
The smallest face-level implementation may be generated from
\[
  \mathbb P_3=\{\mathbf{-1},\mathbf 0,\mathbf{+1}\},
\]
but no physical charge, amplitude, or particle identity is assigned to
these pseudo-values before a \(q\bar r\)-channel package is supplied.

\begin{remark}[Role of \(\mathsf{Pol}\)]
\(\mathsf{Pol}\) denotes the active closed mesonic package space above, and
\(\mathsf{Pol}^{\mathrm{res}}\) adds closed inactive residual packages.
The earlier six-face notation remains useful as a scaffolding, but the
physical mesonic body lives over \(F_6\times\overline{F_6}\).
\end{remark}

\begin{definition}[General Meson]
A General Meson is a two-channel mesonic configuration
\[
  m:A_Y\to\mathsf{Pol}^{\mathrm{res}}.
\]
Equivalently,
\[
  \mathsf{Gen}:=(\mathsf{Pol}^{\mathrm{res}})^{A_Y}.
\]
The faces \(\qu\) and \(\qd\) are the two light-flavor faces of the
Yukawa-style mesonic channel.  They are channel faces, not the two
constituents of a physical meson.
\end{definition}

This definition deliberately keeps the Yukawa-style mediator idea narrow.
It does not say that a meson is universal.  It says only that the basic
mesonic act is readable mediation across the \(\qu,\qd\) light pair.  In
that sense a General Meson is the optimal eye for the nuclear-force
problem: the proton--neutron transition axis is already specified by the
problem itself.
The light-pair configuration is not by itself a derivation of the Yukawa
potential.  That recovery belongs to the low-energy pion channel after the
QCD readout has supplied \(q\bar r\) operators, quantum numbers, and
observables.

\section{The Hidden Cost of Missing Channels}

The decisive distinction is between an inactive channel and a missing
channel.  An inactive channel belongs to the channel basis and carries no
active contribution.  A missing channel has not entered the channel basis.

\[
  \mathbf 0=\text{resolved inactive channel},
  \qquad
  \bot=\text{missing channel}.
\]

\begin{definition}[Inactive-channel provenance]
The symbol \(\mathbf 0\) is a formal null, not a universal physical zero.
When physical input is available, an inactive channel may be refined by a
provenance tag
\[
  \mathbf 0[\rho],
  \qquad
  \rho\in
  \{\mathrm{selection},\mathrm{kinematic},\mathrm{coupling},
    \mathrm{sensitivity},\mathrm{integrated}\}.
\]
Thus a selection-blocked channel, an energetically unreachable channel, a zero
coupling, an experimentally unresolved signal, and an integrated-out
contribution are not collapsed into the same physical claim.
\end{definition}

\begin{definition}[Missing-channel lift]
Let
\[
  \mathsf{Pol}_{\bot}:=\mathsf{Pol}^{\mathrm{res}}\cup\{\bot\}.
\]
The missing-channel lift of a General Meson \(m\in\mathsf{Gen}\) is
\[
  L_{\bot}(m):F_6\to\mathsf{Pol}_{\bot}
\]
defined by
\[
  L_{\bot}(m)(\qu)=m(\qu),\qquad L_{\bot}(m)(\qd)=m(\qd),
\]
and
\[
  L_{\bot}(m)(q)=\bot
  \quad(q\in F_6\setminus A_Y).
\]
\end{definition}

Thus the remaining axes are not false, not zero, and not disproved.  They
are absent from the original face basis.  The two-face theory remains
locally correct while it cannot close propagation that travels
through the missing directions.

\begin{remark}[Physical reading]
The missing faces are not a defect in the original General Meson.  They
are the price of a successful restriction.  A narrow mediator may solve its
local problem precisely because it does not carry the full burden of every
other axis.
\end{remark}

\section{The Grand Meson Face Skeleton}

The first layer of the Grand Meson is obtained by replacing missing flavor
faces with resolved flavor faces.  This is the face skeleton.  It is not
yet the full mesonic operator body; physical mesons enter through the
oriented \(q\bar r\)-channel completion defined later.

\begin{definition}[Grand Meson face skeleton]
A Grand Meson first appears as a six-face mesonic skeleton
\[
  g:F_6\to\mathsf{Pol}^{\mathrm{res}}.
\]
Equivalently,
\[
  \mathsf{Grand}:=(\mathsf{Pol}^{\mathrm{res}})^{F_6}.
\]
The six faces are grouped into three readable axes:
\[
  \qu\leftrightarrow\qd,\qquad
  \qc\leftrightarrow\qs,\qquad
  \qt\leftrightarrow\qb.
\]
These axes are internal flavor-generation axes.  They are not spatial
directions, and they are not quark--quark bound states.  Physical mesons
enter through oriented \(q\bar r\) channels in the QCD readout.
\end{definition}

In this notation the Grand Meson is the admissible \(n=3\) object:
\[
  \mathsf{Grand}=(\mathsf{Pol}^{\mathrm{res}})^{2\cdot 3}.
\]
The adjective ``admissible'' means that all three axes belong to the
channel basis.  It does not mean that every transition is allowed, nor that
every physical theory has been solved.

\begin{definition}[Channel support]
For \(g\in\mathsf{Grand}\), define
\[
  \supp(g)=
  \{q\in F_6\mid g(q)\ne\mathbf 0_q[\rho]\ \text{for every }\rho\}.
\]
A Grand Meson may have inactive channels, but inactivity is resolved by
\(\mathbf 0_q[\rho]\), not by \(\bot\).
\end{definition}

\section{Conservative Recovery}

The Grand Meson must not destroy the General Meson.  It must contain it as
the two-channel face that was already working.

\begin{definition}[Projection to the General Meson]
Define the \(\qu,\qd\)-projection
\[
  p_{\qu\qd}:\mathsf{Grand}\to\mathsf{Gen}
\]
by
\[
  p_{\qu\qd}(g)=g|_{\{\qu,\qd\}}.
\]
\end{definition}

\begin{proposition}[Conservative recovery]
Every General Meson is recovered from any Grand Meson whose \(\qu,\qd\)-face
agrees with it.  In particular, if \(g(\qu)=m(\qu)\) and \(g(\qd)=m(\qd)\), then
\[
  p_{\qu\qd}(g)=m.
\]
\end{proposition}

\begin{proof}
The restriction \(p_{\qu\qd}\) reads exactly the light-pair values.  The
full recovery as a Grand Meson subobject is performed by
\(\beta_{\qu\qd}\), which closes every non-UD face as a buried inactive
package.  Therefore agreement on the two UD channels is sufficient and
necessary for recovery after burial.
\end{proof}

This is the main discipline of the construction.  The Grand Meson does not
rewrite the older meson.  It keeps the working \(\qu\leftrightarrow\qd\)
face and opens two additional quark-pair faces under explicit channel
closure.

\section{Surjective Backward Compatibility}

Backward compatibility must be surjective, not merely suggestive.  Every
General Meson must be obtainable as the visible face of at least one Grand
Meson.

\begin{definition}[Zero extension]
For \(m\in\mathsf{Gen}\), define the zero extension
\[
  z(m):F_6\to\mathsf{Pol}^{\mathrm{res}}
\]
by
\[
  z(m)(\qu)=m(\qu),\qquad z(m)(\qd)=m(\qd),
\]
and
\[
  z(m)(q)=\mathbf 0_q[\mathrm{buried}]
  \qquad(q\in F_6\setminus A_Y).
\]
\end{definition}

\begin{proposition}[Surjective backward map]
The projection \(p_{\qu\qd}:\mathsf{Grand}\to\mathsf{Gen}\) is surjective:
\[
  p_{\qu\qd}\circ z=\mathrm{id}_{\mathsf{Gen}}.
\]
\end{proposition}

\begin{proof}
The zero extension preserves the \(\qu,\qd\)-face of \(m\) and resolves
all other channels as inactive.  Projecting back to the
\(\qu,\qd\)-face therefore returns exactly \(m\).
\end{proof}

\begin{definition}[UD burial normalization]
Define the UD-burial subobject
\[
  \mathsf{Grand}_{\qu\qd}^{0}
  :=
  \{\,g\in\mathsf{Grand}\mid
  g(q)=\mathbf 0_q[\mathrm{buried}]
  \ \text{for every }q\in F_6\setminus A_Y\,\}.
\]
The burial normalization
\[
  \beta_{\qu\qd}:\mathsf{Grand}\longrightarrow\mathsf{Grand}_{\qu\qd}^{0}
\]
is defined by
\[
  \beta_{\qu\qd}(g)(q)=g(q)
  \quad(q\in A_Y),
  \qquad
  \beta_{\qu\qd}(g)(q)=\mathbf 0_q[\mathrm{buried}]
  \quad(q\in F_6\setminus A_Y).
\]
Here \(\mathbf 0_q[\mathrm{buried}]\) is a closed inactive residual package,
not a deletion and not an absent channel.  Other provenance tags describe
other inactive mechanisms, but the canonical recovery of \(\mathsf{Gen}\)
uses the buried tag.
\end{definition}

\begin{proposition}[General Meson as buried Grand Meson]
Restriction to the UD face gives an isomorphism
\[
  \mathsf{Grand}_{\qu\qd}^{0}\cong\mathsf{Gen}.
\]
More explicitly,
\[
  p_{\qu\qd}|_{\mathsf{Grand}_{\qu\qd}^{0}}
  :\mathsf{Grand}_{\qu\qd}^{0}\to\mathsf{Gen}
\]
has inverse \(z:\mathsf{Gen}\to\mathsf{Grand}_{\qu\qd}^{0}\).
\end{proposition}

\begin{proof}
For \(h\in\mathsf{Grand}_{\qu\qd}^{0}\), the non-UD faces are fixed closed
inactive packages, so \(h\) is determined exactly by \(h(\qu)\) and
\(h(\qd)\).  Conversely, \(z(m)\) preserves \(m\) on \(A_Y\) and fills
every other face with a closed inactive package.  Hence
\[
  p_{\qu\qd}\circ z=\mathrm{id}_{\mathsf{Gen}},
  \qquad
  z\circ p_{\qu\qd}
  =
  \mathrm{id}_{\mathsf{Grand}_{\qu\qd}^{0}}.
\]
\end{proof}

Surjectivity gives power: a Grand Meson can always be normalized to an
ordinary General Meson by burying the non-UD faces as closed inactive
packages.  The same operation also carries responsibility, because active
non-UD structure must be closed as residue rather than ignored.

\begin{definition}[Downgrade residue and closure]
For \(g\in\mathsf{Grand}\), define the downgrade residue
\[
  R_{\perp}(g):=g|_{F_6\setminus A_Y}.
\]
For a proposed downgrade symbol \(d:g\Downarrow m\), define
\begin{align*}
  \ClosedResidue_d(R_{\perp}(g))
  \quad\Longleftrightarrow\quad
  &d\ \text{factors through }\beta_{\qu\qd}(g),\\
  &\beta_{\qu\qd}(g)(q)=\mathbf 0_q[\mathrm{buried}]
    \quad \forall q\notin A_Y .
\end{align*}
For a proposed downgrade symbol \(d:g\Downarrow m\), define the Grand
Meson downgrade class
\[
  \Dow_{\mathrm{GM}}(g,m)
  :=
  \{\,d:g\Downarrow m
  \mid
  p_{\qu\qd}(g)=m
  \ \mathrm{and}\
  \ClosedResidue_d(R_{\perp}(g))\,\}.
\]
The contradiction predicate is
\[
  \Contr_{\mathrm{GM}}(d)
  :=
  \bigl(p_{\qu\qd}(g)=m\bigr)
  \wedge
  \bigl(R_{\perp}(g)\not\equiv
    \{\mathbf 0_q[\mathrm{buried}]\}_{q\notin A_Y}\bigr)
  \wedge
  \neg\ClosedResidue_d(R_{\perp}(g)).
\]
\end{definition}

\begin{proposition}[Robust downgrade block]
If \(\Contr_{\mathrm{GM}}(d)\) holds for a proposed downgrade symbol
\(d:g\Downarrow m\), then
\[
  d\notin\Dow_{\mathrm{GM}}(g,m).
\]
If every proposed downgrade symbol from \(g\) to \(m\) is contradictory, then
\[
  \Dow_{\mathrm{GM}}(g,m)=\varnothing .
\]
\end{proposition}

\begin{proof}
Membership in \(\Dow_{\mathrm{GM}}(g,m)\) requires the residue predicate
\[
  \ClosedResidue_d(R_{\perp}(g)).
\]
The predicate \(\Contr_{\mathrm{GM}}(d)\) contains its negation.  The two
conditions cannot hold simultaneously, hence \(d\) is not a Grand Meson
downgrade.  If no proposed symbol satisfies the membership condition, the
class is empty.
\end{proof}

\begin{remark}[Fusion and fission]
Fusion is the closed downgrade: the active extra axes are collapsed into
the General Meson face only with an explicit residual channel.  Fission is
not a second operation.  It is the name of the contradictory downgrade
pattern in which the same projection is written while the extra axes are
discarded as if they had never contributed.  In the Grand Meson channel
discipline, this pattern is blocked by emptiness of
\(\Dow_{\mathrm{GM}}(g,m)\).  The word does not denote nuclear fission in
this paper.
\end{remark}

\section{QCD Completion as Channel Indexing}

The Grand Meson completes the QCD mesonic flavor sector only in the
following strict sense: it adds a recoverable generation-axis indexing layer
to the standard color-singlet quark--antiquark bilinear.  It does not alter
QCD color dynamics, does not replace confinement, and does not claim a new
bound state by notation alone.  Its completion operation is
\[
  O_{q\bar r}\longmapsto(i,j,O_{q\bar r}),
  \qquad q\in A_i,\ r\in A_j,
\]
together with the recovery map that forgets \((i,j)\).  Let the three
flavor axes be
\[
  A_1=\{\qu,\qd\},\qquad
  A_2=\{\qc,\qs\},\qquad
  A_3=\{\qt,\qb\}.
\]

\begin{definition}[Grand Meson channel completion]
The physical Grand Meson completion is the \(36\)-channel object
\[
  \mathsf{GM}_{36}
  :=
  \left\{
    (i,j,P_{q\bar r})
    \ \middle|\
    q\in A_i,\ r\in A_j,\
    P_{q\bar r}\in\mathsf{Pol}^{\mathrm{res}}_{q\bar r},\
    \Cl(P_{q\bar r})=\Cl_{\mathrm{GM}}
  \right\}.
\]
Equivalently, a completed Grand Meson is a map
\[
  G:F_6\times\overline{F_6}\longrightarrow
  \bigsqcup_{q,r\in F_6}\mathsf{Pol}^{\mathrm{res}}_{q\bar r},
  \qquad
  G(q,\bar r)=P_{q\bar r}.
\]
The six-face skeleton records the available flavor faces; the
\(F_6\times\overline{F_6}\) completion is the mesonic channel body.
\end{definition}

\begin{definition}[UD burial on the channel body]
Let
\[
  \mathsf{Map}_{36}
  :=
  \{\,G:F_6\times\overline{F_6}\to
  \bigsqcup_{q,r\in F_6}\mathsf{Pol}^{\mathrm{res}}_{q\bar r}
  \mid
  (i,j,G(q,\bar r))\in\mathsf{GM}_{36}
  \text{ whenever }q\in A_i,\ r\in A_j\,\}.
\]
Let
\[
  \mathsf{GM}_{36,\qu\qd}^{0}
  :=
  \{\,G\in\mathsf{Map}_{36}\mid
  G(q,\bar r)=\mathbf 0_{q\bar r}[\mathrm{buried}]
  \ \text{for every }(q,\bar r)\notin
  A_Y\times\overline{A_Y}\,\}.
\]
Define the channel-body burial normalization
\[
  \mathcal B_{\qu\qd}:\mathsf{Map}_{36}\to\mathsf{GM}_{36,\qu\qd}^{0}
\]
by
\[
  \mathcal B_{\qu\qd}(G)(q,\bar r)
  =
  \begin{cases}
    G(q,\bar r), & (q,\bar r)\in A_Y\times\overline{A_Y},\\
    \mathbf 0_{q\bar r}[\mathrm{buried}],
      & (q,\bar r)\notin A_Y\times\overline{A_Y}.
  \end{cases}
\]
Thus every cross-generation or non-UD body channel is retained as a closed
inactive residual package, not deleted and not forgotten.
\end{definition}

\begin{proposition}[Body-level General Meson recovery]
Let
\[
  \mathsf{Gen}^{\mathrm{body}}_{\qu\qd}
  :=
  \prod_{q,r\in A_Y}\mathsf{Pol}^{\mathrm{res}}_{q\bar r}
\]
be the oriented UD mesonic body.  Restriction to
\(A_Y\times\overline{A_Y}\) gives
\[
  \mathsf{GM}_{36,\qu\qd}^{0}
  \cong
  \mathsf{Gen}^{\mathrm{body}}_{\qu\qd}.
\]
\end{proposition}

\begin{proof}
Every channel outside \(A_Y\times\overline{A_Y}\) is fixed to the closed
inactive package \(\mathbf 0_{q\bar r}[\mathrm{buried}]\), so an element of
\(\mathsf{GM}_{36,\qu\qd}^{0}\) is determined exactly by its four oriented
UD channels:
\[
  \qu\bar{\qu},\quad
  \qu\bar{\qd},\quad
  \qd\bar{\qu},\quad
  \qd\bar{\qd}.
\]
Conversely, any package assignment on these four channels extends uniquely
by buried inactive packages on the remaining \(32\) channels.
\end{proof}

\begin{definition}[Generation as internal direction]
Let
\[
  G_3=\{1,2,3\},
  \qquad
  H_2=\{+,-\}.
\]
Define
\[
  \Phi:G_3\times H_2\longrightarrow F_6
\]
by
\[
  (1,+)\mapsto\qu,\quad (1,-)\mapsto\qd,\quad
  (2,+)\mapsto\qc,\quad (2,-)\mapsto\qs,\quad
  (3,+)\mapsto\qt,\quad (3,-)\mapsto\qb .
\]
Then
\[
  A_i=\Phi(\{i\}\times H_2).
\]
\end{definition}

\begin{proposition}[Three generations are Grand Meson directions]
The six-flavor Grand Meson decomposes as
\[
  \mathsf{Grand}
  =
  (\mathsf{Pol}^{\mathrm{res}})^{F_6}
  \cong
  (\mathsf{Pol}^{\mathrm{res}})^{G_3\times H_2}
  \cong
  \left((\mathsf{Pol}^{\mathrm{res}})^{H_2}\right)^{G_3}.
\]
Thus the three observed quark generations are not external decorations of
the six-face skeleton.  They are the three internal directions of the Grand
Meson, each carrying one up-type and one down-type face.
\end{proposition}

\begin{definition}[Top-axis closure]
The third direction
\[
  A_3=\{\qt,\qb\}
\]
is not an ordinary top-meson axis.  It is a weak-virtual channel axis.
For a channel \(q\bar r\), define
\[
  \Top(q\bar r)
  \quad\Longleftrightarrow\quad
  (q=\qt)\ \text{or}\ (r=\qt).
\]
An ordinary mesonic readout is defined only when \(\Top(q\bar r)\) is
false.  If \(\Top(q\bar r)\) is true, the channel closes only through a
weak-virtual channel closure
\[
  \mathsf{WCl}(q\bar r)\in\RCl(q\bar r).
\]
The closure object is the weak charged-current exit available before an
ordinary top-bearing mesonic readout can be formed:
\[
  \mathsf{WCl}(\qt\bar r)
  :=
  \{\,\qt\to d_j W^+\ \mathrm{with}\ V_{3j}\mid j=1,2,3\,\},
\]
and, for an anti-top-bearing channel,
\[
  \mathsf{WCl}(q\bar{\qt})
  :=
  \{\,\bar{\qt}\to\bar d_j W^-\ \mathrm{with}\ \overline{V_{3j}}
    \mid j=1,2,3\,\}.
\]
Thus the top axis is present in the six-flavor completion, but ordinary
mesonic readability is blocked unless the weak-virtual closure is supplied.
\end{definition}

\begin{proposition}[Unclosed top readout is GM-contradictory]
Let \((i,j,O_{q\bar r})\) be a Grand Meson lifted channel.  If
\(\Top(q\bar r)\) holds and \(\mathsf{WCl}(q\bar r)\notin\RCl(q\bar r)\), then the
ordinary-meson readout symbol is contradictory.  Equivalently,
\[
  \Top(q\bar r)
  \ \wedge\
  \mathsf{WCl}(q\bar r)\notin\RCl(q\bar r)
  \quad\Longrightarrow\quad
  \Contr_{\mathrm{GM}}(i,j,O_{q\bar r}).
\]
\end{proposition}

\begin{definition}[Anti-face fission]
Let the opposite face on the same generation axis be
\[
  \op(\Phi(i,\epsilon)):=\Phi(i,-\epsilon),
  \qquad i\in G_3,\ \epsilon\in H_2 .
\]
For a mesonic channel \(O_{q\bar r}\), the anti-quark side is the charge
conjugate \(\bar r\).  It is not the opposite Grand Meson face
\(\overline{\op(r)}\).  Define the anti-face fission candidate by
\[
  \Fis_{\bar F}(O_{q\bar r})
  :=
  O_{q\,\overline{\op(r)}} .
\]
This symbol is not a new physical process.  It is the diagnostic pattern in
which the anti-quark side of a channel is replaced by the opposite
Grand Meson face on the same generation axis.
\end{definition}

\begin{proposition}[Anti-face fission is a Grand Meson contradiction]
Let
\[
  \kappa(q,\bar r):=(Q_q-Q_r,\ e_q-e_r)
\]
be the charge--flavor fingerprint of the mesonic channel, where \(Q_q\) is
the electric charge of \(q\) and \(e_q\) is its flavor unit.  If
\[
  R_{\bar F}(q,\bar r)
  :=
  \kappa(q,\bar r)-\kappa(q,\overline{\op(r)})
  \ne 0
\]
then identifying \(O_{q\bar r}\) with \(\Fis_{\bar F}(O_{q\bar r})\) is
contradictory:
\[
  O_{q\bar r}\equiv \Fis_{\bar F}(O_{q\bar r})
  \quad\Longrightarrow\quad
  \Contr_{\mathrm{GM}}(q,\bar r).
\]
\end{proposition}

\begin{proof}
The Grand Meson readout preserves the ordered pair consisting of the quark
side and the anti-quark side.  Replacing \(\bar r\) by
\(\overline{\op(r)}\) changes the channel fingerprint by
\(R_{\bar F}(q,\bar r)\).  When this residual fingerprint is nonzero, the
two channels lie in distinct charge--flavor sectors, so the proposed
identification cannot inhabit the Grand Meson readout space.
\end{proof}

\begin{remark}[Examples]
For the light pion channel, anti-face fission sends
\[
  O_{\qu\bar{\qd}}
  \longmapsto
  O_{\qu\bar{\qu}} .
\]
The electric charge changes from \(Q_{\qu}-Q_{\qd}=+1\) to
\(Q_{\qu}-Q_{\qu}=0\), so the residual fingerprint is nonzero.  For a
cross-generation channel,
\[
  O_{\qu\bar{\qs}}
  \longmapsto
  O_{\qu\bar{\qc}},
\]
the same block occurs: \(Q_{\qu}-Q_{\qs}=+1\), whereas
\(Q_{\qu}-Q_{\qc}=0\).  On the third axis,
\[
  O_{\qu\bar{\qb}}
  \longmapsto
  O_{\qu\bar{\qt}},
\]
the flipped readout also enters the top-axis closure condition above.
Thus the Grand Meson does more than re-label QCD flavor bookkeeping: it
detects a concrete wrong anti-face selection and leaves it outside every
well-formed downgrade or ordinary readout.  The flipped symbol may only be
treated as a distinct channel with its own package and quantum fingerprint.
\end{remark}

\begin{remark}[Worked channel table]
The following examples show that the completion is operational rather than
merely decorative.  ``Ord.'' means ordinary hadronic meson readout, and
\(R_{\perp}\) is the residual axis content under downgrade to the
\(\qu\leftrightarrow\qd\) face.
\begin{center}
\small
\begin{tabular}{@{}llllll@{}}
channel & \(q\bar r\) & axes & ord. & top & anti-face test / \(R_{\perp}\)\\
\hline
\(\pi^+\) & \(\qu\bar{\qd}\) & \((1,1)\) & yes & no
  & \(\qu\bar{\qu}:\ Q +1\to0;\ R_{\perp}=0\)\\
\(K^+\) & \(\qu\bar{\qs}\) & \((1,2)\) & yes & no
  & \(\qu\bar{\qc}:\ Q +1\to0;\ R_{\perp}=A_2\)\\
\(D^0\) & \(\qc\bar{\qu}\) & \((2,1)\) & yes & no
  & \(\qc\bar{\qd}:\ Q 0\to+1;\ R_{\perp}=A_2\)\\
\(D_s^+\) & \(\qc\bar{\qs}\) & \((2,2)\) & yes & no
  & \(\qc\bar{\qc}:\ Q +1\to0;\ R_{\perp}=A_2\)\\
\(B^-\) & \(\qb\bar{\qu}\) & \((3,1)\) & yes & no
  & \(\qb\bar{\qd}:\ Q -1\to0;\ R_{\perp}=A_3\)\\
\(J/\psi\) & \(\qc\bar{\qc}\) & \((2,2)\) & yes & no
  & \(\qc\bar{\qs}:\ Q 0\to+1;\ R_{\perp}=A_2\)\\
\(\Upsilon\) & \(\qb\bar{\qb}\) & \((3,3)\) & yes & no
  & \(\qb\bar{\qt}:\ Q 0\to-1;\ R_{\perp}=A_3\)\\
\(\qt\bar{\qb}\) & \(\qt\bar{\qb}\) & \((3,3)\) & no & \(\mathsf{WCl}\)
  & \(\qt\bar{\qt}:\ Q +1\to0;\ R_{\perp}=A_3\)\\
\(\qu\bar{\qt}\) & \(\qu\bar{\qt}\) & \((1,3)\) & no & \(\mathsf{WCl}\)
  & \(\qu\bar{\qb}:\ Q 0\to+1;\ R_{\perp}=A_3\)
\end{tabular}
\end{center}
The first seven rows are ordinary QCD mesonic readouts after closure.  The
last two are not ordinary hadronic mesons in this framework; they are
top-bearing weak-virtual channels.  In every row, the anti-face candidate
changes the charge--flavor sector, so an attempted identification is a
\(\Contr_{\mathrm{GM}}\) witness unless the residual channel is explicitly
closed.
\end{remark}

\begin{definition}[Grand Meson CKM lift]
Let
\[
  U_L=(\qu_L,\qc_L,\qt_L)^T,\qquad
  D_L=(\qd_L,\qs_L,\qb_L)^T,
\]
and let \(V_{\mathrm{CKM}}=(V_{ij})\in U(3)\) be the charged-current
flavor-mixing matrix.  The ordinary weak current is
\[
  J^\mu_+
  =
  \bar U_L\gamma^\mu V_{\mathrm{CKM}}D_L
  =
  \sum_{i,j=1}^{3}V_{ij}\,\bar u_{iL}\gamma^\mu d_{jL}.
\]
The Grand Meson lift places each CKM coefficient on its axis pair:
\[
  \Gamma_{\mathrm{GM}}^{\mathrm{CKM}}(V_{ij})
  :=
  (i,j,V_{ij}).
\]
\end{definition}

\begin{proposition}[CKM recovery]
Let \(\pi_{\mathrm{CKM}}(i,j,V_{ij})=V_{ij}\).  Then forgetting the Grand
Meson axis labels recovers the ordinary CKM current:
\[
  \sum_{i,j}\pi_{\mathrm{CKM}}
  \bigl(\Gamma_{\mathrm{GM}}^{\mathrm{CKM}}(V_{ij})\bigr)
  \bar u_{iL}\gamma^\mu d_{jL}
  =
  J^\mu_+ .
\]
Thus the Grand Meson does not derive the CKM matrix.  It annotates the
charged-current terms by generation-axis labels and verifies that forgetting
those labels recovers the ordinary weak current.  CKM mixing is therefore a
required weak-current closure layer, not a modification of QCD color
dynamics.
\end{proposition}

The ordinary QCD mesonic operator basis is
\[
  \mathsf Q_{\mathrm{mes}}
  :=
  \left\{
    O^{\Gamma_\sigma}_{q\bar r}(x)
    =
    \sum_{a=1}^{3}\bar r_a(x)\Gamma_\sigma q^a(x)
    \ \middle|\ q,r\in F_6,\ \sigma\ \text{a spin-parity sector}
  \right\}.
\]
Standard channels are recovered inside this basis, for example
\[
  \pi^+\sim O_{\qu\bar{\qd}},\quad
  K^+\sim O_{\qu\bar{\qs}},\quad
  D^0\sim O_{\qc\bar{\qu}},\quad
  B^-\sim O_{\qb\bar{\qu}},
\]
and
\[
  J/\psi\sim O_{\qc\bar{\qc}},
  \qquad
  \Upsilon\sim O_{\qb\bar{\qb}},
  \qquad
  \eta,\eta'\sim
  \mathrm{span}\{O_{\qu\bar{\qu}},O_{\qd\bar{\qd}},O_{\qs\bar{\qs}}\}.
\]
Thus cross-generation mesons such as \(u\bar s\), \(c\bar d\), \(b\bar u\),
and \(c\bar s\) are ordinary members of the same basis.
The Grand Meson lift is
\[
  \Gamma_{\mathrm{GM}}(O_{q\bar r})
  :=
  (i,j,O_{q\bar r})
  \quad
  \text{where }q\in A_i,\ r\in A_j.
\]
Thus
\[
  \mathsf M_{\mathrm{GM}}
  :=
  \{\,(i,j,O_{q\bar r})\mid q\in A_i,\ r\in A_j,\ 1\le i,j\le3\,\}.
\]

\begin{proposition}[Conservative QCD completion]
Let
\[
  \pi_{\mathrm{QCD}}(i,j,O_{q\bar r})=O_{q\bar r}.
\]
Then
\[
  \pi_{\mathrm{QCD}}\circ\Gamma_{\mathrm{GM}}
  =
  \mathrm{id}_{\mathsf Q_{\mathrm{mes}}}.
\]
Therefore the Grand Meson is a conservative completion of the QCD mesonic
flavor sector: it adds the three-axis pair closure \((i,j)\) while
preserving the color-singlet QCD operator.
\end{proposition}

\begin{remark}[Canonical completion property]
The object \(\mathsf{GM}_{36}\) is used here as the canonical conservative
completion of \(\mathsf Q_{\mathrm{mes}}\) satisfying the following
conditions:
\begin{enumerate}[label=(\roman*)]
  \item each \(O_{q\bar r}\) has the unique generation-axis pair
    \((i,j)\) determined by \(q\in A_i\) and \(r\in A_j\);
  \item forgetting the axis pair and package data recovers \(O_{q\bar r}\);
  \item anti-face replacement and top-bearing ordinary readout produce
    \(\Contr_{\mathrm{GM}}\) unless their residual channels close;
  \item no new color representation or color force is introduced;
  \item the QCD quantum numbers, source Lagrangian, correlators, and CKM
    charged-current layer are preserved under forgetting.
\end{enumerate}
This is a canonical labeling and closure property, not a category-theoretic
universal property in an unintroduced category.  If a later version defines
such a category of completions, the required comparison map would have the
form
\[
  \theta:\mathsf{GM}_{36}\longrightarrow\mathcal X
\]
with
\[
  \widehat{\Gamma}_{\mathrm{GM}}(O_{q\bar r})=(i,j,P_{q\bar r}),
  \qquad
  \pi_{\mathrm{QCD}}\circ\theta\circ\widehat{\Gamma}_{\mathrm{GM}}
  =
  \mathrm{id}_{\mathsf Q_{\mathrm{mes}}}.
\]
\end{remark}

\begin{definition}[Grand Meson source Lagrangian]
Write the quark-sector QCD Lagrangian as
\[
  \mathcal L_{\mathrm{QCD}}
  =
  -\frac14G^A_{\mu\nu}G^{A\mu\nu}
  +
  \sum_{q\in F_6}\bar q(i\gamma^\mu D_\mu-m_q)q .
\]
The Grand Meson source lift is
\[
  \mathcal L_{\mathrm{GM}}[J]
  :=
  \mathcal L_{\mathrm{QCD}}
  +
  \sum_{i,j=1}^{3}
  \sum_{\substack{q\in A_i\\ r\in A_j}}
  \sum_{\sigma}
  J_{ij,\sigma}^{q\bar r}\,O^{\Gamma_\sigma}_{q\bar r}.
\]
Here \(J_{ij,\sigma}^{q\bar r}\) is not a new color force.  It is the
closed Grand Meson source attached to the axis pair \((i,j)\) and
spin-parity sector \(\sigma\).
\end{definition}

\begin{proposition}[Lagrangian recovery]
Setting all Grand Meson sources to zero recovers QCD:
\[
  \mathcal L_{\mathrm{GM}}[0]=\mathcal L_{\mathrm{QCD}}.
\]
Forgetting the axis-pair labels sends the lifted source terms back to the
ordinary mesonic source sector
\[
  \sum_{q,r\in F_6}\sum_{\sigma}
  J_{\sigma}^{q\bar r}O^{\Gamma_\sigma}_{q\bar r}.
\]
Thus the Grand Meson completes the mesonic Lagrangian source layer by
adding axis-channel closure, not by altering the color-gauge core.
\end{proposition}

\begin{definition}[Grand Meson quantum numbers]
Let \(Q_q\) be the electric charge of quark \(q\), and let
\(e_q\in\mathbb Z^{F_6}\) be the flavor unit at channel \(q\).  For a
color-singlet mesonic channel with spin-parity label \(\sigma\), define
\[
  \nu_{\mathrm{QCD}}(q,\bar r;\sigma)
  :=
  (\sigma,\ Q_q-Q_r,\ e_q-e_r,\ \mathsf B_{\mathrm{ary}}=0,\
  \mathbf 1_{\mathrm c}).
\]
The Grand Meson lift is
\[
  \nu_{\mathrm{GM}}(q,\bar r;\sigma)
  :=
  (i,j,\nu_{\mathrm{QCD}}(q,\bar r;\sigma)),
  \qquad q\in A_i,\ r\in A_j .
\]
Here \(\sigma\) may be \(J^{PC}\) for neutral channels; for charged or
open-flavor channels it is the corresponding spin-parity sector without an
unavailable charge-conjugation label.
\end{definition}

\begin{proposition}[Quantum-number recovery]
If \(\pi_\nu(i,j,\nu)=\nu\), then
\[
  \pi_\nu\circ\nu_{\mathrm{GM}}
  =
  \nu_{\mathrm{QCD}}.
\]
Thus the Grand Meson completes mesonic quantum numbers by adding only
the axis-pair quantum number \((i,j)\), while preserving charge, flavor,
baryon number, color singletness, and the spin-parity sector.
\end{proposition}

\begin{definition}[Grand Meson observables]
For QCD mesonic operators \(O^{\Gamma_\sigma}_{q\bar r}\), write the
ordinary two-point observable as
\[
  C^{\sigma\tau}_{q\bar r,s\bar t}(x-y)
  :=
  \left\langle
  O^{\Gamma_\sigma}_{q\bar r}(x)
  \bigl(O^{\Gamma_\tau}_{s\bar t}(y)\bigr)^{\dagger}
  \right\rangle .
\]
The Grand Meson lift is the axis-closed observable
\[
  C^{\mathrm{GM},\sigma\tau}_{ij,kl}(q\bar r,s\bar t;x-y)
  :=
  (i,j,k,l,\ C^{\sigma\tau}_{q\bar r,s\bar t}(x-y)),
\]
where \(q\in A_i,\ r\in A_j,\ s\in A_k,\ t\in A_l\).
\end{definition}

\begin{proposition}[Observable recovery]
Let \(\pi_{\mathrm{obs}}(i,j,k,l,C)=C\).  Then
\[
  \pi_{\mathrm{obs}}
  \bigl(C^{\mathrm{GM},\sigma\tau}_{ij,kl}(q\bar r,s\bar t;x-y)\bigr)
  =
  C^{\sigma\tau}_{q\bar r,s\bar t}(x-y).
\]
Thus masses, widths, and spectral poles read from ordinary QCD correlators
are preserved, while the Grand Meson retains which axis-pair channel made
the observable readable.
\end{proposition}

\begin{definition}[Grand Meson conservation law]
Let \(\mathbf a_{ij}\) be the unit basis element of
\(\mathbb Z^{3\times3}\).  For \(q\in A_i\) and \(r\in A_j\), define
\[
  K_{\mathrm{QCD}}(q\bar r)
  :=
  (Q_q-Q_r,\ e_q-e_r,\ \mathsf B_{\mathrm{ary}}=0,\
  \mathbf 1_{\mathrm c})
\]
and lift it to
\[
  K_{\mathrm{GM}}(q\bar r)
  :=
  (\mathbf a_{ij},\ K_{\mathrm{QCD}}(q\bar r)).
\]
\end{definition}

\begin{proposition}[Conservation-law recovery]
For an oriented mesonic process
\(\mathcal P=\sum_{\alpha}\eta_{\alpha}(q_\alpha\bar r_\alpha)\), ordinary
QCD conservation is
\[
  \sum_{\alpha}\eta_{\alpha}
  K_{\mathrm{QCD}}(q_\alpha\bar r_\alpha)=0 .
\]
The Grand Meson conservation condition is
\[
  \sum_{\alpha}\eta_{\alpha}
  K_{\mathrm{GM}}(q_\alpha\bar r_\alpha)
  =
  (R_{\mathrm{axis}}(\mathcal P),0).
\]
Thus QCD conservation is recovered by forgetting the axis residue
\(R_{\mathrm{axis}}\).  A closed Grand Meson process has
\(R_{\mathrm{axis}}=0\); otherwise the nonzero axis residue must be
closed rather than ignored.
\end{proposition}

At this level QCD becomes concise: its mesonic flavor sector is the union
of one-axis terms \((i=i)\) and two-axis terms \((i\ne j)\) among the
three Grand Meson axes.  The dynamics of color is not discarded; it is the
singlet contraction already present in \(O_{q\bar r}\).  Channels touching
\(\qt\) are retained as weak-virtual channels satisfying closure, not as ordinary
top mesons.

\section{Einstein's Gate}

The Grand Meson must also avoid the classical aether failure.  It is not a
hidden medium with an absolute state of rest.  It is a local six-face
mediator with a \(36\)-channel oriented body whose outputs must be readable
without introducing an absolute frame.
Einstein's Gate is not a claim of Einstein's endorsement.  It names the
closure condition inherited from relativity: no mesonic theory may
reintroduce an absolute hidden medium.

\begin{axiom}[Einstein's Gate: no absolute mesonic frame]
No equation in the Grand Meson theory may depend on a privileged global
rest frame of the mediator.  Only resolved flavor channels, local channel closure, and
observer-readable projections may be used.
For a Lorentz transformation \(\Lambda\), the flavor-axis labels
\((i,j)\) are internal labels and remain fixed, while spacetime arguments
and tensor indices transform in the ordinary way:
\[
  (i,j,O^{\Gamma_\sigma}_{q\bar r}(x))
  \longmapsto
  (i,j,O^{\prime\,\Gamma'_\sigma}_{q\bar r}(\Lambda x)).
\]
Observable statements must therefore be Lorentz scalars or covariant tensor
statements after forgetting the internal Grand Meson labels.
The source layer must satisfy
\[
  \mathcal L_{\mathrm{GM}}[J](x)
  \longmapsto
  \mathcal L'_{\mathrm{GM}}[J'](\Lambda x)
  =
  \mathcal L_{\mathrm{GM}}[J](x),
\]
and the lifted correlator must transform only through its spacetime
arguments and tensor indices:
\[
  C^{\mathrm{GM},\sigma\tau}_{ij,kl}(x-y)
  \longmapsto
  C_{\mathrm{GM}}^{\prime\,ij,kl,\sigma'\tau'}(\Lambda(x-y)).
\]
Thus \((i,j)\) are internal scalar labels, not aether-like spacetime
degrees of freedom.
\end{axiom}

\begin{remark}[Observer-side fission test]
Let a concept or entity \(X\) be represented by a closed Grand Meson state
\(g_X\).  A thought experiment may introduce a proposed fission symbol
\(d:g_X\Downarrow m_X\) and later reclose the discussion by fusion.  This
does not damage the theory when the whole argument remains inside
\(\Mor_{\mathrm{GM}}\).  If, however, the proposed fission leaves a
contradiction witness
\[
  \Contr_{\mathrm{GM}}(d),
\]
then the symbol has already left the Grand Meson transition space.  Any
later observer class \(\mathcal O\) may then split the reading of \(X\)
into a closed reading and a fission-intent reading:
\[
  \Contr_{\mathrm{GM}}(d)
  \quad\Longrightarrow\quad
  \exists r_1,r_2\in\Read_{\mathcal O}(X)
  \ \text{with}\ r_1\ne r_2 .
\]
The point is not an external second act.  The point is that an unclosed
anti-face fission, once it leaves the category of the argument, is already
enough to split the observer-side readout.  Einstein's Gate therefore
requires thought experiments about fission to close before projection to
observers.
\end{remark}

This axiom is Einstein's Gate.  It does not ban mediation.  It bans
absolute mediation.  The Grand Meson is therefore not a return to the
classical aether.  It is a channel-resolved local mediator that must survive
projection into closed observations.

\section{Why \texorpdfstring{\(n=3\)}{n=3} Is the Minimal Complete Cover}

Formally one may define an \(n\)-meson by
\[
  \mathsf{Mes}_n:=(\mathsf{Pol}^{\mathrm{res}})^{2n}.
\]
The case \(n=1\) is the General Meson.  The case \(n=3\) is the Grand
Meson.

The reason to stop at \(n=3\) is not that higher \(n\) is impossible, and
it is not a proof of why nature has exactly three generations.  It is that
the known six-flavor quark sector already factors as
\[
  F_6\cong G_3\times H_2 .
\]
Therefore \(n=3\) is the exact internal-generation cover of the present
mesonic flavor sector:
\[
  \Read_{G_3}(\mathsf{Mes}_3)
  =
  \Read_{G_3}((\mathsf{Pol}^{\mathrm{res}})^{G_3\times H_2}).
\]
For \(n>3\), additional axes require additional readout structure.  Such
structure may be meaningful in a later theory, but it is not required for
the present six-flavor sector:
\[
  \Read_{G_3}(\mathsf{Mes}_n)
  \not>
  \Read_{G_3}(\mathsf{Mes}_3)
  \qquad(n>3).
\]

Thus \(n=3\) is the minimal complete cover for the present Grand Meson
input: complete because it covers the known three-generation quark sector,
minimal because no fourth axis is introduced without a corresponding
standard-model flavor input.  This is not a theorem explaining why nature
has three generations.  It is the closure statement for the currently used
six-flavor quark input; if the input sector changes, the update procedure
must be rerun rather than silently extended.

\section{Closed Transition}

Grand Meson robustness is not the absence of change.  It is the closure of
every changed channel.  Let a proposed transition symbol be written
\[
  u:g\to g'.
\]
Define its changed-channel support by
\[
  \Delta_{\mathrm{ch}}(u)
  :=
  \{q\in F_6\mid g(q)\ne g'(q)\}.
\]

\begin{definition}[Grand Meson transition space]
The Grand Meson transition space is
\[
  \Mor_{\mathrm{GM}}
  :=
  \{\,u:g\to g'
  \mid
  \Delta_{\mathrm{ch}}(u)\subseteq\RCl(u)\,\}.
\]
The contradiction predicate for a proposed transition symbol is
\[
  \Contr_{\mathrm{GM}}(u)
  :=
  \Delta_{\mathrm{ch}}(u)\setminus\RCl(u)\ne\varnothing .
\]
\end{definition}

\begin{proposition}[Transition block]
If \(\Contr_{\mathrm{GM}}(u)\), then
\[
  u\notin\Mor_{\mathrm{GM}}.
\]
Consequently a channel-boundary crossing without closure does not name an
operation inside the Grand Meson theory; it is a contradiction witness
outside the transition space.
\end{proposition}

\begin{proof}
By definition, \(u\in\Mor_{\mathrm{GM}}\) requires
\(\Delta_{\mathrm{ch}}(u)\subseteq\RCl(u)\).  The predicate
\(\Contr_{\mathrm{GM}}(u)\) states that
\(\Delta_{\mathrm{ch}}(u)\setminus\RCl(u)\) is nonempty.  These conditions are
inconsistent, so the proposed symbol cannot inhabit \(\Mor_{\mathrm{GM}}\).
\end{proof}

\begin{definition}[Physical well-formedness]
A physically well-formed Grand Meson transition must satisfy
\[
  \mathrm{PhysWF}(u)
  :=
  \bigl(u\in\Mor_{\mathrm{GM}}\bigr)
  \wedge
  \mathrm{Cons}_{\mathrm{QCD}}(u)
  \wedge
  \mathrm{Lag}_{\mathrm{GM}}(u)
  \wedge
  \mathrm{Obs}_{\mathrm{GM}}(u).
\]
Here \(\mathrm{Cons}_{\mathrm{QCD}}\) means that the conservation condition
closes, \(\mathrm{Lag}_{\mathrm{GM}}\) means that the transition has a
defined Lagrangian-source layer, and \(\mathrm{Obs}_{\mathrm{GM}}\) means
that an observable readout exists.
\end{definition}

Equivalently,
\[
  \text{non-transition}=\text{unclosed discontinuity}.
\]
The discontinuity itself is not the contradiction.  Without channel closure,
the corresponding transition symbol has no place in \(\Mor_{\mathrm{GM}}\).

\section{Conclusion}

The Grand Meson is not proposed as an omnipotent meson.  It is proposed as
a closed \(n=3\) general-purpose meson:
\[
  \mathsf{Grand}=(\mathsf{Pol}^{\mathrm{res}})^{6}.
\]
It conservatively recovers the General Meson by a surjective projection,
replaces missing channels with resolved flavor channels, avoids an absolute aether frame,
and treats robustness as channel closure rather than immobility.  Its backward
compatibility is therefore responsible: extra axes may be fused into the
older face only with a residual channel closure, while unclosed fission
reduces to \(\Contr_{\mathrm{GM}}\) and has an empty transition class.

The path is therefore narrow.  Begin with the \(\qu\leftrightarrow\qd\)
eye that already works.  Open \(\qc\leftrightarrow\qs\) and
\(\qt\leftrightarrow\qb\) without breaking it.  Stop at \(n=3\) until a
future theory can make higher axes readable without turning them into a
burden of infinite explanation.

\section*{Acknowledgement}

The author acknowledges Hideki Yukawa, whose meson theory supplies the
foundational mediator problem from which this construction begins.  The
author also acknowledges Albert Einstein, whose relativistic limit on
classical mediation clarifies the boundary region exposed by the theoretical
dialogue with Yukawa's mesonic idea.  Finally, the author thanks OpenAI and
GPT-5.5 for helping weave this idea into a coherent manuscript and for
respecting the strict confidentiality required during the drafting process.

\begin{thebibliography}{9}

\bibitem{Yukawa1935}
H. Yukawa.
On the interaction of elementary particles.
\emph{Proceedings of the Physico-Mathematical Society of Japan},
17:48--57, 1935.

\bibitem{Einstein1905}
A. Einstein.
On the electrodynamics of moving bodies.
\emph{Annalen der Physik}, 17:891--921, 1905.

\end{thebibliography}

\end{document}
